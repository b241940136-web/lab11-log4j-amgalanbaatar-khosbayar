# Алхам 4 — app.log файлын командын мөрийн шинжилгээ

## Ашигласан командууд ба гаралт

---

### 1. Бүх ERROR мөрийг шүүх

```bash
grep "ERROR" logs/app.log
```

**Гаралт (жишээ):**
```
2026-04-15 10:23:45 [main] ERROR BankAccount:72 - Амжилтгүй: үлдэгдэл хүрэлцэхгүй! requestedAmount=99999.0, currentBalance=600.0, accountId=ACC-001
2026-04-15 10:23:45 [main] ERROR Customer:54 - getDomain() алдаа: email null байна — NullPointerException-аас урьдчилан сэргийлж байна
```

---

### 2. Туршилтын үеэр лог урсгалыг шууд харах

```bash
tail -f logs/app.log
```

**Тайлбар:** Програм ажиллаж байх үед лог файлд шинэ мөр нэмэгдэх бүрт терминалд шууд гарна. Ctrl+C-ээр зогсооно.

---

### 3. tail ба grep-ийг нэгтгэн WARN мөр бүртгэх

```bash
tail -f logs/app.log | grep "WARN"
```

**Гаралт (жишээ):**
```
2026-04-15 10:23:45 [main] WARN  BankAccount:35 - Буруу оролт: amount -50.0 сөрөг байна, accountId=ACC-001
2026-04-15 10:23:45 [main] WARN  BankAccount:56 - Буруу оролт: amount -10.0 сөрөг байна, accountId=ACC-001
2026-04-15 10:23:45 [main] WARN  Customer:36 - Customer үүсгэхэд email null байна — getDomain() ажиллахгүй
```

---

### 4. awk ашиглан log level-ийн баганыг гаргах

```bash
awk '{print $3}' logs/app.log | sort | uniq -c
```

**Гаралт (жишээ):**
```
     3 DEBUG
     2 ERROR
     1 FATAL
     5 INFO
     4 TRACE
     3 WARN
```

**Тайлбар:** Гурав дахь багана (`$3`) нь log level агуулна. `sort | uniq -c` нь давтагдах тоог тоолно.

---

### 5. Тодорхой цаг мөчид үүссэн үйл явдлыг шүүх

```bash
grep "2026-04-15 10:" logs/app.log
```

**Гаралт (жишээ):**
```
2026-04-15 10:23:44 [main] INFO  Main:28 - ===== Лаб 11 — Log4j Debug Туршилт эхэлж байна =====
2026-04-15 10:23:44 [main] INFO  BankAccount:20 - BankAccount үүсгэгдлээ: accountId=ACC-001, initialBalance=500.0
2026-04-15 10:23:45 [main] INFO  BankAccount:45 - Deposit амжилттай: amount=200.0, шинэ balance=700.0, accountId=ACC-001
...
```

---

## Даалгавар 4.1 — Яагаад `tail -f` нь `cat` / `less`-ээс debug-д илүү хэрэгтэй вэ?

`tail -f` нь файл нээлттэй байхдаа шинэ мөр нэмэгдэх бүрт **шууд дэлгэцэнд харуулдаг**, тиймээс програм ажиллаж байх үед логийн урсгалыг **бодит цаг хугацаанд** ажиглах боломжтой. Харин `cat` нь тухайн мөчид файлд байгаа агуулгыг нэг удаа хэвлэж зогсдог тул шинэ лог мөр нэмэгдсэнийг харж чадахгүй. `less` нь файлыг хэсгэхэн унших боломж өгдөг ч шинэ мөрийг автоматаар дагадаггүй (хэдийгээр `Shift+F` дарахад дагах боломж бий). Debug хийхэд програмын хариу үйлдлийг **шууд харах** шаардлагатай тул `tail -f` хамгийн тохиромжтой.
